# Cortex — Multimodal RAG
# Managed by Pipeline Lead. Other team members: post requests in team chat.
# Install: uv pip install --python .venv -r requirements.txt

# --- LLM serving client (vLLM runs externally; this is the SDK only) ---
openai>=1.0,<2.0

# --- LlamaIndex orchestration ---
llama-index>=0.12,<1.0
llama-index-embeddings-huggingface>=0.5,<1.0
llama-index-vector-stores-chroma>=0.4,<1.0
llama-index-llms-openai-like>=0.4,<1.0

# --- Vector store ---
chromadb>=0.6,<1.0

# --- PDF processing ---
PyMuPDF>=1.24,<2.0

# --- Embeddings + reranking ---
sentence-transformers>=3.0,<4.0
rank-bm25>=0.2,<1.0

# --- Evaluation ---
ragas>=0.2,<1.0
datasets>=3.0,<4.0
langchain-openai>=0.1,<1.0

# --- UI (UI Lead) ---
streamlit>=1.40,<2.0

# --- Utilities ---
python-dotenv>=1.0,<2.0
pillow>=10.0,<12.0
